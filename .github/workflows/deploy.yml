name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - 'PUBLISH.md'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v5

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # events.json から index.html / sitemap.xml / robots.txt を生成する。
      # SITE_URL は Pages の公開URLが自動で入るので、手で設定する必要はない。
      - name: Build site
        env:
          SITE_URL: ${{ steps.pages.outputs.base_url }}
        run: python3 build_site.py

      - name: Assemble _site
        run: |
          mkdir -p _site/data
          cp index.html og.png sitemap.xml robots.txt _site/
          cp data/events.json _site/data/
          touch _site/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
